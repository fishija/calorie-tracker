[project]
name = "llm-calorie-tracker"
version = "0.1.0"
description = "LLM-powered calorie estimation and tracking application."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
prod = [
    "gunicorn>=26.0.0",
]
